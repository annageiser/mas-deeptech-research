# vX.Y.Z — short title

**Date:** YYYY-MM-DD
**Git tag:** `vX.Y.Z`
**Commits in this iteration:** `<sha-range>`

## Problem

What was wrong / missing before this iteration. State it as the symptom a user would see, then the root cause.

## Under the Hood

What changed in the codebase + the rationale. Cite specific files and the key design decision (not just "we added X" — explain why X was the right shape, what alternatives were rejected, what the trade-off is).

## Next Steps

Concrete follow-ups this iteration unlocks (or explicitly defers). One bullet per step. Include a "deferred" sub-list for items that this iteration's design depends on but didn't land.
